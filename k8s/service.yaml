apiVersion: v1
kind: Service
metadata:
  name: finance-tracker-app-service
spec:
  type: LoadBalancer
  selector:
    app: finance-tracker-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
